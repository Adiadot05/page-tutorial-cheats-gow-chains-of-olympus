function copyToClipboard1() {
    // Ambil elemen code
    const codeElement = document.getElementById('code1');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button1');
            const copyIcon = document.getElementById('copy-icon1');
            const copyText = document.getElementById('copy-text1');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard2() {
    // Ambil elemen code
    const codeElement = document.getElementById('code2');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button2');
            const copyIcon = document.getElementById('copy-icon2');
            const copyText = document.getElementById('copy-text2');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard3() {
    // Ambil elemen code
    const codeElement = document.getElementById('code3');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button3');
            const copyIcon = document.getElementById('copy-icon3');
            const copyText = document.getElementById('copy-text3');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard4() {
    // Ambil elemen code
    const codeElement = document.getElementById('code4');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button4');
            const copyIcon = document.getElementById('copy-icon4');
            const copyText = document.getElementById('copy-text4');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard5() {
    // Ambil elemen code
    const codeElement = document.getElementById('code5');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button5');
            const copyIcon = document.getElementById('copy-icon5');
            const copyText = document.getElementById('copy-text5');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard6() {
    // Ambil elemen code
    const codeElement = document.getElementById('code6');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button6');
            const copyIcon = document.getElementById('copy-icon6');
            const copyText = document.getElementById('copy-text6');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard7() {
    // Ambil elemen code
    const codeElement = document.getElementById('code7');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button7');
            const copyIcon = document.getElementById('copy-icon7');
            const copyText = document.getElementById('copy-text7');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}

function copyToClipboard8() {
    // Ambil elemen code
    const codeElement = document.getElementById('code8');
    
    // Buat teks yang disalin dari konten elemen code
    const codeText = codeElement.innerText;

    // Salin teks ke clipboard
    navigator.clipboard.writeText(codeText)
        .then(() => {
            // Ganti ikon dan teks tombol
            const copyButton = document.getElementById('copy-button8');
            const copyIcon = document.getElementById('copy-icon8');
            const copyText = document.getElementById('copy-text8');
            
            copyIcon.classList.remove('bi-copy');
            copyIcon.classList.add('bi-check2');
            copyText.textContent = 'Disalin';

            // Reset tombol setelah 3 detik
            setTimeout(() => {
                copyIcon.classList.remove('bi-check2');
                copyIcon.classList.add('bi-copy');
                copyText.textContent = 'Copy Code';
            }, 1000);
        })
        .catch(err => {
            console.error('Failed to copy text: ', err);
        });
}